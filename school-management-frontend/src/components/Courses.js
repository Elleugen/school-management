import React from 'react';
import CourseList from './CourseList';

const Courses = () => {
  return (
    <div className="courses">
      <h2>Courses</h2>
      <CourseList />
    </div>
  );
};

export default Courses;
